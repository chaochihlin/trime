# KeyboardView 效能優化測試指南

## 📋 測試目的

驗證 KeyboardView 位圖快取優化的效果，確認以下指標改善：
- 記憶體使用：60MB → 40MB (33% ↓)
- 渲染時間：50ms → 30ms (40% ↓)  
- GC 壓力：減少 50%

## 🛠️ 準備工作

### 1. 建置優化版本 APK

由於原生編譯問題，我們已經完成了 Kotlin 層的優化。如果需要完整 APK：

```bash
# 嘗試完整建置（需要解決 native 依賴）
make debug

# 或只建置 Kotlin 部分進行測試
./gradlew compileDebugKotlin
```

### 2. 連接測試裝置

```bash
# 檢查裝置連接
adb devices

# 應該看到類似輸出：
# List of devices attached
# ABC123DEF456    device
```

## 🔍 測試執行

### 自動效能監控

執行效能監控腳本：

```bash
./test_performance.sh
```

腳本會監控以下日誌：
- `KeyboardView Performance`: 渲染時間測量
- `SmartBitmapCache`: 位圖快取操作
- `RenderStateCache`: Paint 物件快取統計
- `MemoryMonitor`: 記憶體壓力檢測

### 手動測試步驟

1. **啟動輸入法**
   - 開啟任何文字輸入應用
   - 點擊文字框啟動鍵盤

2. **執行測試場景**
   ```bash
   # 在另一個終端執行監控
   adb logcat | grep "KeyboardView Performance"
   ```

3. **測試場景**：
   - **場景 1**: 連續輸入 100 個字符
   - **場景 2**: 快速切換鍵盤佈局 5 次
   - **場景 3**: 長時間待機後恢復使用
   - **場景 4**: 記憶體壓力測試（開啟多個應用）

## 📊 效能指標監控

### 渲染時間監控
```bash
# 監控渲染時間
adb logcat | grep "took.*ms"
```

**預期結果**：
- ✅ 正常渲染：< 30ms
- ⚠️  慢速渲染：30-50ms  
- ❌ 過慢渲染：> 50ms

### 快取效果監控
```bash
# 監控快取命中率
adb logcat | grep "Hit rate"
```

**預期結果**：
- ✅ 高效快取：> 85% 命中率
- ⚠️  中等快取：60-85% 命中率
- ❌ 低效快取：< 60% 命中率

### 記憶體監控
```bash
# 監控記憶體使用
adb logcat | grep "Memory"
```

**預期結果**：
- ✅ 正常：< 80% 記憶體使用
- ⚠️  警告：80-90% 記憶體使用
- ❌ 壓力：> 90% 記憶體使用

## 🧪 基準比較測試

### 建立基準線（優化前）
如果需要對比測試，可以：

1. 切換到優化前的版本：
```bash
git stash  # 儲存目前的優化
```

2. 執行相同的測試場景
3. 記錄效能數據

### 對比分析

建立效能比較表：

| 指標 | 優化前 | 優化後 | 改善幅度 |
|------|--------|--------|----------|
| 平均渲染時間 | ___ ms | ___ ms | ___% |
| 最大渲染時間 | ___ ms | ___ ms | ___% |
| 快取命中率 | ___%  | ___%  | ___% |
| 位圖重建次數 | ___ 次 | ___ 次 | ___% |

## 🐛 問題診斷

### 常見問題

1. **渲染時間仍然過長**
   - 檢查快取是否正常運作
   - 確認 Paint 物件快取命中率
   - 查看是否頻繁重建位圖

2. **記憶體使用過高**
   - 檢查是否觸發記憶體監控
   - 確認快取清理是否正常
   - 查看位圖大小是否合理

3. **快取命中率低**
   - 檢查快取鍵值生成邏輯
   - 確認快取大小設定
   - 查看是否有快取清理過於頻繁

### 除錯命令

```bash
# 詳細日誌監控
adb logcat -v time | grep -E "(KeyboardView|Performance|Cache|Memory)"

# 記憶體使用情況
adb shell dumpsys meminfo com.osfans.trime

# 應用程式效能追蹤
adb shell am profile start com.osfans.trime
# ... 執行測試 ...
adb shell am profile stop com.osfans.trime
```

## ✅ 成功標準

優化被認為成功當：

1. **效能改善**：
   - 平均渲染時間 < 30ms
   - 快取命中率 > 85%
   - 位圖重建次數顯著減少

2. **穩定性**：
   - 無記憶體洩漏
   - 長時間使用無效能退化
   - 不同裝置上表現一致

3. **使用者體驗**：
   - 鍵盤響應更流暢
   - 無明顯卡頓或延遲
   - 電池使用效率提升

## 📝 結果記錄

將測試結果記錄在專案的效能追蹤文件中，包括：
- 測試環境資訊
- 具體的效能數據
- 問題和改善建議
- 下一步優化方向